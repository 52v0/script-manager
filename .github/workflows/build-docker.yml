<<<<<<< HEAD
name: Build and Push Docker Image

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: 52v0/script-manager:latest
          platforms: linux/amd64,linux/arm64
=======
name: Build Docker Image

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Build Docker image
      run: |
        docker build -t script-manager:latest .
        docker save -o script-manager.tar script-manager:latest
        gzip script-manager.tar
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: script-manager-image
        path: script-manager.tar.gz
        retention-days: 30
>>>>>>> c86f52865b58c9111e64f45628d81b9434f8e191
